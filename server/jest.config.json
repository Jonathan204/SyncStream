{
  "testEnvironment": "node",
  "verbose": true,
  "setupFiles": ["dotenv/config"],
  "preset": "ts-jest",
  "transform" : {
    "^.+\\.(ts|tsx)?$": "ts-jest",
    "^.+\\.(js|jsx)$": "babel-jest"
  }
}